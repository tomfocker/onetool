# CapsWriter 备用源码

这里保存的是从 `onetool` 主应用中移除的 `CapsWriter` 语音输入相关源码。

移除原因：

- 当前实现依赖外部固定目录 `C:\CapsWriter-Offline`
- 单独安装 `onetool` 时该功能默认不可用
- `v0.1.0` 首发不再保留默认不可工作的功能入口

当前这些文件不参与主应用构建，仅作为后续重构或重新集成时的参考备份。
